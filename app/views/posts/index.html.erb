<p id="notice"><%= notice %></p>
<div class="row row-eq-height">
      <div class="column1_inner">
       <p> Что должно быть в новостях в боковой колонке? </p>
      </div>

<div class="column2_inner">
<h1>News</h1>


 
    <% @posts.each do |post| %>
     <div class='news' >
      <h2> <%= link_to post.title , post %> </h2>
      <p><%= post.datenews.to_formatted_s(:default) if post.datenews? %></p>
      <p><%= post.body.html_safe %></p>
      
         
      <% if logged_in? %>
      <p><%= link_to 'Edit', edit_post_path(post) %> |
      <%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></p>
      <% end %>
    
    </div>

      
     
    <% end %>
    <br>
    <br>
    <% if logged_in? %>
      <%= link_to 'New Post', new_post_path %>
    <% end %>
</div>
</div>



