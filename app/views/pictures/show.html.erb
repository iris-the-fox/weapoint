
    <%= @picture.firearm_id %>
  
    <%= @picture.title %>
    <%= image_tag( @picture.image.url(:large)) %>
    <%= @picture.author %>
    <%= @picture.summary %>
    
<%= link_to 'Edit', edit_picture_path(@picture) %> |
<%= link_to 'Back', pictures_path %>
